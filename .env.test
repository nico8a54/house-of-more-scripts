# House of More — Test Member Credentials
# Never commit this file.
# Load with: node --env-file=.env.test test-profiles.js
# Requires Node 20+. Alternatively: export $(cat .env.test | grep -v ^#) && node test-profiles.js

# ── Member A (existing test account) ──────────────────────────────────────────
TEST_MEMBER_ID=mem_sb_cmla331hr05l50sps7d2rhy5q
TEST_MEMBER_EMAIL=test@example.com
TEST_MEMBER_NAME=Test Member A
TEST_RECORD_ID=rec_test_member

# ── Shared event to stress test ───────────────────────────────────────────────
TEST_EVENT_ID=rec_test_event

# ── Member B (create in Memberstack sandbox, run questionnaire to get record) ──
TEST_MEMBER_ID_2=mem_sb_REPLACE_WITH_REAL_ID
TEST_MEMBER_EMAIL_2=testb@example.com
TEST_MEMBER_NAME_2=Test Member B
TEST_RECORD_ID_2=rec_REPLACE_WITH_REAL_RECORD

# ── Member C (same steps as B) ────────────────────────────────────────────────
TEST_MEMBER_ID_3=mem_sb_REPLACE_WITH_REAL_ID
TEST_MEMBER_EMAIL_3=testc@example.com
TEST_MEMBER_NAME_3=Test Member C
TEST_RECORD_ID_3=rec_REPLACE_WITH_REAL_RECORD
